// services/snapshot/snapshotScheduler.js
export function scheduleAutoSnapshot(assetId, frequency = 'daily') {
    // Placeholder for future snapshot logic (e.g. background job or cron trigger)
    console.log(`[snapshot] Scheduled snapshot for asset ${assetId} every ${frequency}`);
  }
